import React from 'react';
import { Row } from 'react-bootstrap';

const Loading = () => (
  <Row className="app-chrome vertical-align-container">
    <p className="loading text-center vertical-align-item">submitting your information...</p>
  </Row>
);

export default Loading;